好，下面给你一份“每一步都有命令”的完整版本。你可以直接照着执行。

一、准备阶段（只做一次）
1. 设置 Git 身份
    git config --global user.name "你的名字"
    git config --global user.email "你的邮箱"

2. 建议的全局设置
    git config --global pull.rebase true
    git config --global init.defaultBranch main

3. 检查是否设置成功
    git config --global --list

二、进入你的项目目录
1. 进入项目
    cd ~/你的项目目录

2. 确认这里是 Git 仓库
    git rev-parse --is-inside-work-tree

如果输出 true，说明目录正确。

三、场景 A：本地已有代码，但还没连接远程仓库
1. 初始化（如果还没 init）
    git init

2. 查看状态
    git status -sb

3. 暂存所有改动
    git add -A

4. 提交第一次代码
    git commit -m "chore: initial commit"

5. 添加远程仓库地址（把 URL 换成你的）
    git remote add origin https://github.com/你的用户名/你的仓库.git

6. 查看远程是否添加成功
    git remote -v

7. 推送到远程并建立跟踪
    git branch -M main
    git push -u origin main

四、场景 B：远程仓库已存在，直接克隆
1. 克隆仓库
    git clone https://github.com/你的用户名/你的仓库.git

2. 进入仓库目录
    cd 你的仓库

3. 查看分支和远程
    git branch -vv
    git remote -v

五、日常开发流程（最常用）
1. 先同步远端最新代码
    git pull --rebase

2. 查看本地改动
    git status -sb
    git diff

3. 暂存改动
    git add -A

4. 提交改动
    git commit -m "fix: 简要说明本次修改"

5. 推送改动
    git push

六、推荐分支开发（更规范）
1. 从主分支切新分支
    git switch main
    git pull --rebase
    git switch -c feat/功能名

2. 开发后提交
    git add -A
    git commit -m "feat: 新功能说明"

3. 首次推送该分支
    git push -u origin feat/功能名

4. 后续继续推送
    git push

七、多远程协作（你现在这种）
假设你有三个远程：origin、upstream、suyouli。

1. 查看远程
    git remote -v

2. 设置默认推送到你的仓库远程
    git config remote.pushDefault suyouli

3. 验证默认推送远程
    git config --get remote.pushDefault

4. 以后直接推送
    git push

八、常见问题与对应命令
1. 报错：不是 git 仓库
    pwd
    cd ~/正确的仓库目录
    ls -la .git

2. 报错：push 被拒绝
    git pull --rebase
    git push

3. 提交了但 GitHub 看不到
先确认你看的分支对不对，再执行：
    git branch -vv
    git log --oneline -n 5
    git push

4. 想看哪些文件已暂存
    git diff --cached --name-only

九、一套最短可记忆命令（你平时就用这 4 行）
    git pull --rebase
    git add -A
    git commit -m "你的提交信息"
    git push

十、VS Code 操作留存（中文版）
1. 打开正确仓库目录
    文件 -> 打开文件夹 -> 选择你的仓库根目录（要能看到 .git）

2. 进入源代码管理
    左侧活动栏点击“源代码管理”（分支图标）

3. 查看并暂存改动
    单文件：文件右侧点击“+”（暂存更改）
    全部文件：点击“暂存所有更改”

4. 提交
    在顶部输入框写提交信息
    点击“提交”

5. 推送
    点击“同步更改”或“推送”

6. 新建分支（推荐）
    左下角分支名 -> “创建新分支” -> 输入分支名（如 feat/camera-fix）
    首次推送后会自动建立远程跟踪分支

7. 发起 PR（拉取请求）
    推送后如果右下角弹出“创建拉取请求”，直接点击
    或在源代码管理面板中点击“创建拉取请求”
    目标分支选 humble，来源分支选你的功能分支

8. 提交前 10 秒自检
    看左下角当前分支是否正确
    看变更列表里是否有不该提交的文件
    确认提交信息能说明“改了什么、为什么改”

9. 常见报错（VS Code 点击版）
    Push 被拒绝：先点“拉取（变基）”，再点“推送”
    有冲突：按冲突提示选择并保存，完成后再提交并推送
    看不到 Git 面板：确认打开的是仓库根目录，不是上一级普通目录

10. 一键节奏（图形化）
    源代码管理 -> 暂存所有更改 -> 提交 -> 同步更改
